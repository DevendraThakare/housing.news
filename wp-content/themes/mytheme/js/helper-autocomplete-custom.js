/**
 * Twenty Sixteen keyboard support for image navigation.
 */

( function( $ ) {
	var tax = $(this).closest('div.tagsdiv').attr('id');
				$('#new-tag-post_tag').focus(function(){
alert('hi');
				});
} )( jQuery );
